# Moved to standalone repo

Nova punch-QA workflow now lives in **[nova-punch-qa](https://github.com/meerkat-commits/pd-os)**.

Use that repo for:

- `./setup/bin/run.sh` daily captures
- `shots.json`, `issues.md`, `qa-test-cases.md`
- `python3 -m nova_qa.cli run`

This folder is kept in pd-os as a pointer only. Do not add new workflow files here.
